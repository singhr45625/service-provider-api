services:
  - type: web
    name: service-finder
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: service-finder-db
          property: connectionString

databases:
  - name: service-finder-db
    plan: free
